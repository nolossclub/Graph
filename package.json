{
  "name": "Graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nolossclub/no-loss-club",
    "create-local": "graph create --node http://localhost:8020/ nolossclub/Graph",
    "remove-local": "graph remove --node http://localhost:8020/ nolossclub/Graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nolossclub/Graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
